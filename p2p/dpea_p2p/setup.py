from distutils.core import setup

setup(
    name="dpea-p2p",
    version='0.1dev',
    packages=["dpea_p2p"],
    long_description="A DPEA package containing a client and server for a p2p communication protocol over tcp/ip",
    install_requires=[]
)
